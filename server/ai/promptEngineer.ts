export function generateIntentPrompt(question: string): string {
  return `
    Ты — Алуа, помощник для пользователей маркетплейса Satori.
    Проанализируй вопрос пользователя: "${question}".
    Определи его намерение (intent) и извлеки ключевые данные:
      - intent: одно из значений: "status" (статус заказа), "items" (состав заказа), "pickup" (адрес самовывоза), "unknown" (неизвестное намерение).
      - orderId: номер заказа (если указан). Если номер заказа отсутствует, верни null.
    Верни ответ СТРОГО в формате JSON, без объяснений или дополнительного текста.
    Примеры:
    Вопрос: "Где мой заказ SAT-2024-5678?"
    Ответ:
    \`\`\`json
    {
      "intent": "status",
      "orderId": "SAT-2024-5678"
    }
    \`\`\`
    Вопрос: "Что я заказал?"
    Ответ:
    \`\`\`json
    {
      "intent": "items",
      "orderId": null
    }
    \`\`\`
    Вопрос: "Как приготовить борщ?"
    Ответ:
    \`\`\`json
    {
      "intent": "unknown",
      "orderId": null
    }
    \`\`\`
  `;
}

export function generateResponsePrompt(intent: string, context?: any): string {
  const allowedTopics = `
    Разрешённые темы: заказы, профиль, информация о маркетплейсе Satori, ошибки на сайте.
    Если вопрос выходит за рамки этих тем, ответь: "Извините, я могу помочь только с вопросами о заказах, профиле, информации о маркетплейсе Satori или ошибках на сайте. Пожалуйста, задайте вопрос по этим темам! 😊"
  `;

  const orderContext = context?.type === 'order' ? `
    Контекст заказа: ${JSON.stringify(context.data)}.
    Если вопрос связан с заказом, используй этот контекст для ответа.
  ` : '';

  return `
    ${allowedTopics}
    ${orderContext}
    Намерение пользователя: "${intent}".
    Сформируй ответ на основе намерения и контекста.
    Используй простой и понятный язык, добавляй эмодзи для дружелюбия.
  `;
}